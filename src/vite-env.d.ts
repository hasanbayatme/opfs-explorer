/// <reference types="vite/client" />

/**
 * Injected at build time by Vite's `define` config from package.json.
 * Always reflects the canonical version — no manual updates needed.
 */
declare const __APP_VERSION__: string;
